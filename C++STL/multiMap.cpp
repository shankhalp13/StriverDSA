#include <bits/stdc++.h>

using namespace std;

int main()
{
    //will be sorted
    //everything is same as map, only thing is it can store duplicate keys
    //only mpp[key] cannot be used here
    return 0;
}
