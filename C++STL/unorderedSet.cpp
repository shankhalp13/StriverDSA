#include <bits/stdc++.h>

using namespace std;

//store unique but is not sorted
int main(){
    //all operation is similar to set except it is not sorted

    //O(1) but can be O(N) in worst case only
}